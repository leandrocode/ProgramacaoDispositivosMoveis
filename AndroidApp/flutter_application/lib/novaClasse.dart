import 'dart:html';
import 'package:flutter/cupertino.dart';

class NovaClasse extends StatelessWidget {
  NovaClasse({key? key}) : super(key: key);
  @Override
  Widget build(BuildContext context) {
    return layout;
  }
}
